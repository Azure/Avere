# Terraform backend configuration for state file storage
resource_group_name  = "AzureRender" // Set to the same securityResourceGroupName value in variables.tf
storage_account_name = "azrender"    // Set to the same securityStorageAccountName value in variables.tf
container_name       = "terraform"   // Set to the same terraformStorageContainerName value in variables.tf
