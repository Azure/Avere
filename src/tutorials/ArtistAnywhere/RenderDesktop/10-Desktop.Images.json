{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "renderDesktop": {
      "type": "object",
      "metadata": {
        "description": "The render client desktop."
      }
    },
    "imageGallery": {
      "type": "object",
      "metadata": {
        "description": "The shared image gallery."
      }
    },
    "virtualNetwork": {
      "type": "object",
      "metadata": {
        "description": "The virtual network subnet."
      }
    }
  },
  "variables": {
    "imageGalleryApiVersion": "2019-12-01",
    "imageBuilderApiVersion": "2019-05-01-preview"
  },
  "resources": [
    {
      "condition": "[parameters('renderDesktop').machineImages[copyIndex()].enabled]",
      "type": "Microsoft.VirtualMachineImages/imageTemplates",
      "name": "[parameters('renderDesktop').machineImages[copyIndex()].templateName]",
      "apiVersion": "[variables('imageBuilderApiVersion')]",
      "location": "[resourceGroup().location]",
      "properties": {
        "vmProfile": {
          "vnetConfig": "[parameters('virtualNetwork')]"
        },
        "buildTimeoutInMinutes": "[parameters('renderDesktop').machineImages[copyIndex()].buildTimeoutMinutes]",
        "source": {
          "type": "[parameters('renderDesktop').machineImages[copyIndex()].sourceType]",
          "version": "[parameters('renderDesktop').machineImages[copyIndex()].sourceVersion]",
          "publisher": "[reference(resourceId('Microsoft.Compute/galleries/images', parameters('imageGallery').name, parameters('renderDesktop').machineImages[copyIndex()].definitionName), variables('imageGalleryApiVersion')).identifier.publisher]",
          "offer": "[reference(resourceId('Microsoft.Compute/galleries/images', parameters('imageGallery').name, parameters('renderDesktop').machineImages[copyIndex()].definitionName), variables('imageGalleryApiVersion')).identifier.offer]",
          "sku": "[reference(resourceId('Microsoft.Compute/galleries/images', parameters('imageGallery').name, parameters('renderDesktop').machineImages[copyIndex()].definitionName), variables('imageGalleryApiVersion')).identifier.sku]"
        },
        "customize": "[if(equals(length(parameters('renderDesktop').machineImages[copyIndex()].buildCustomization), 0), json('null'), parameters('renderDesktop').machineImages[copyIndex()].buildCustomization)]",
        "distribute": [
          {
            "type": "SharedImage",
            "runOutputName": "SharedImage",
            "galleryImageId": "[resourceId('Microsoft.Compute/galleries/images', parameters('imageGallery').name, parameters('renderDesktop').machineImages[copyIndex()].definitionName)]",
            "replicationRegions": "[parameters('imageGallery').imageReplicationRegions]",
            "artifactTags": {
              "imageTemplate": "[parameters('renderDesktop').machineImages[copyIndex()].templateName]"
            }
          }
        ]
      },
      "copy": {
        "name": "renderDesktops",
        "count": "[length(parameters('renderDesktop').machineImages)]"
      }
    }
  ],
  "outputs": {
    "renderDesktopImages": {
      "type": "array",
      "value": "[parameters('renderDesktop').machineImages]"
    }
  }
}
